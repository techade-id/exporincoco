ADMIN_USER=admin
ADMIN_PASSWORD=change-me
ADMIN_SECRET=

# Google Cloud Storage — keeps editorial text and image uploads after deploys
GCS_BUCKET=exporincoco
GCS_PROJECT_ID=
# Service account key JSON (raw or base64). Leave empty to use Application Default Credentials.
GCS_CREDENTIALS=

# Optional alternative to GCS
GITHUB_TOKEN=
GITHUB_REPO=techade-id/exporincoco
GITHUB_BRANCH=main
